export const bookProductionSchema = [
  `CREATE TABLE IF NOT EXISTS classroom_book_settings (classroom_id TEXT PRIMARY KEY REFERENCES classrooms(id) ON DELETE CASCADE, grade INTEGER, class_number INTEGER, rubric_json TEXT, rubric_version TEXT, rubric_filename TEXT, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
  `CREATE TABLE IF NOT EXISTS book_feedback_jobs (id TEXT PRIMARY KEY, storybook_id TEXT NOT NULL REFERENCES storybooks(id) ON DELETE CASCADE, classroom_id TEXT NOT NULL REFERENCES classrooms(id) ON DELETE CASCADE, teacher_id TEXT NOT NULL REFERENCES teachers(id) ON DELETE CASCADE, revision INTEGER NOT NULL, rubric_version TEXT NOT NULL, rubric_json TEXT NOT NULL, prompt TEXT NOT NULL, document_json TEXT NOT NULL, title TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'queued', feedback_json TEXT, error TEXT, lease TEXT, attempts INTEGER NOT NULL DEFAULT 0, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE(storybook_id, teacher_id, revision, rubric_version))`,
  `CREATE INDEX IF NOT EXISTS book_feedback_jobs_queue ON book_feedback_jobs(teacher_id, classroom_id, status, created_at)`,
  `CREATE TABLE IF NOT EXISTS book_print_jobs (id TEXT PRIMARY KEY, storybook_id TEXT NOT NULL REFERENCES storybooks(id) ON DELETE CASCADE, classroom_id TEXT NOT NULL REFERENCES classrooms(id) ON DELETE CASCADE, teacher_id TEXT NOT NULL REFERENCES teachers(id) ON DELETE CASCADE, revision INTEGER NOT NULL, environment TEXT NOT NULL, spec_uid TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'queued', book_uid TEXT, layout_json TEXT, error TEXT, lease TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE(storybook_id, teacher_id, revision, environment, spec_uid))`,
  `CREATE TABLE IF NOT EXISTS book_print_orders (id TEXT PRIMARY KEY, teacher_id TEXT NOT NULL REFERENCES teachers(id) ON DELETE CASCADE, classroom_id TEXT NOT NULL REFERENCES classrooms(id) ON DELETE CASCADE, environment TEXT NOT NULL, request_json TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'submitting', response_json TEXT, error TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`,
];
